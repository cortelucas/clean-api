{
  "name": "clean-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --watchAll",
    "test:staged": "NODE_OPTIONS=--experimental-vm-modules npx jest --passWithNoTests",
    "prepare": "husky install",
    "lint-staged": "lint-staged",
    "lint:fix": "standard --fix"
  },
  "jest": {
    "transform": {},
    "coverageDirectory": "coverage",
    "testEnvironment": "node"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "lint-staged": {
    "*.js": [
      "npm run lint:fix",
      "npm run test:staged",
      "git add"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "husky": "^8.0.3",
    "jest": "^29.5.0",
    "lint-staged": "^13.2.2",
    "standard": "^17.0.0"
  }
}
